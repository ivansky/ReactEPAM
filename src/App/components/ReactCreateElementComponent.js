import React from 'react';

const reactCreateElementComponent = () => React.createElement(
    'div',
    null,
    `Hello, this is React.createElement component`
);

export default reactCreateElementComponent;
